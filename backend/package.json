{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.53.0",
    "@aws-sdk/util-dynamodb": "^3.53.0",
    "random-words": "^1.1.2",
    "shared": "file:../shared"
  },
  "scripts": {
    "start": "docker run -d -p 8000:8000 --name dynamodb amazon/dynamodb-local && serverless offline --stage=dev",
    "deploy": "serverless deploy",
    "destroy": "serverless remove"
  }
}
